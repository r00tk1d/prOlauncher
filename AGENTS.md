# Instructions for AI agents

- Whenever you make a user-facing change to this project (a new feature, a change in behavior, or a visible bug fix), also update `README.md` so it stays in sync.
- Keep the "Added features" section in `README.md` accurate: describe new features, and adjust existing entries if their behavior changed.
- Do not remove or rewrite the reference to the original upstream repo (`tanujnotes/Olauncher`) unless asked.
